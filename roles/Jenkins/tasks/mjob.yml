- name: sjobs Create .xml file
  community.general.jenkins_job:
    config: "{{ lookup('file', 'templates/mjobs.xml') }}"
    name: sjobs.xml
    password: admin123
    url: http://:8080
    user: admin


- name: copy sjobs.xml file
  ansible.builtin.template:
    name: mjobs.xml
    dest: /var/lib/jenkins

- name: sjobs Create .xml file
  community.general.jenkins_job:
    config: "{{ lookup('file', 'templates/mjobs.xml') }}"
    name: sjobs.xml
    password: admin123
    url: http://:8080
    user: admin